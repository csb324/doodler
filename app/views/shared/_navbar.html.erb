<nav class="top-bar">
  <div class="row navbar-row">
  <ul class="title-area">
    <li class="name"><h1 class="sketch"><%= link_to 'DoodleMission', root_path %></h1></li>
  </ul>

  <section class="top-bar-section">
    <ul class="right">

      <% if user_signed_in? %>
        <li><%= link_to "Signed in as #{current_user.nickname}", user_path(current_user) %> </li>
        <li><%= link_to 'Sign out', destroy_user_session_path, method: :delete %></li>
      <% else %>
        <li>
          <%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook) %>
        </li>
      <% end %>

    </ul>
  </section>
  </div>

</nav>
