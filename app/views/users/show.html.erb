<div class="row">
  <div class="column">
    <h1><%= @user.nickname %></h1>
    <p><%= @user.points %> points</p>
    <p>Doodler since <%= @user.created_at.strftime("%B %e, %Y") %></p>
  </div>
</div>
